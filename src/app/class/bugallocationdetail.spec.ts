import { Bugallocationdetail } from './bugallocationdetail';

describe('Bugallocationdetail', () => {
  it('should create an instance', () => {
    expect(new Bugallocationdetail()).toBeTruthy();
  });
});
